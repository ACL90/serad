## Test environments
- local Windows 10, R 4.x
- devtools::check(cran = TRUE)

## R CMD check results
There were no ERRORs or WARNINGs.

## Downstream dependencies
There are currently no downstream dependencies.

## Comments
This is the initial CRAN submission of the package.

The package provides tools for generating dynamic written outputs
in French and English based on predefined writing rules.

R-hub and win-builder checks could not be completed due to
connection timeouts, but all local checks passed successfully.
